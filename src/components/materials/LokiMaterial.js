import { Clock, DoubleSide, RawShaderMaterial } from "three"; import { LokiMatShader } from "./LokiMatShader"; class LokiMaterial extends RawShaderMaterial { constructor(e = {}) { super({ vertexShader: LokiMatShader.vertexShader, fragmentShader: LokiMatShader.fragmentShader, transparent: !0, depthWrite: !1, side: DoubleSide }), this.uniforms = e, this.clock = new Clock } onUpdate() { this.uniforms.time.value += this.clock.getDelta() } } export { LokiMaterial };